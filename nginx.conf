# ============================================================
# 客服工单系统 nginx 配置
# 将下面的 location 块加入服务器已有的 nginx 配置中
# 客服系统运行在 Docker 容器中，监听 127.0.0.1:8000
# ============================================================

# 在已有的 server {} 块中添加以下内容：

client_max_body_size 20m;

# ===== 客服工单系统（添加到已有 server 块中）=====
location /customer-service/ {
    rewrite ^/customer-service(.*)$ $1 break;
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location /static/ {
    proxy_pass http://127.0.0.1:8000/static/;
    proxy_set_header Host $host;
}

location /api/ {
    proxy_pass http://127.0.0.1:8000/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;  # AI 回复可能较慢
}

# 如果希望客服系统独占整个域名/端口，改用下面独立 server 块：
#
# server {
#     listen 8080;  # 或其他空闲端口
#     server_name _;
#     client_max_body_size 20m;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
